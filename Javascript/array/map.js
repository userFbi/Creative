let data = [10, 20, 30, 40, 50];
console.log(data);

//Map
let updatedData = data.map((el) => el * 2);
console.log(updatedData);
1;

// filter
let filteredData = data.filter((el) => el < 50);
console.log(filteredData);
